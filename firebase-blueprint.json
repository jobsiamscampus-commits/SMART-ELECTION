{
  "entities": {
    "students": {
      "title": "Students",
      "description": "Student roster for election verification",
      "properties": {
        "studentId": { "type": "string" },
        "name": { "type": "string" },
        "department": { "type": "string" },
        "password": { "type": "string" },
        "photo": { "type": "string" },
        "hasVoted": { "type": "boolean" }
      }
    },
    "votes": {
      "title": "Votes",
      "description": "Submitted ballot votes",
      "properties": {
        "studentId": { "type": "string" },
        "candidateId": { "type": "string" },
        "position": { "type": "string" }
      }
    }
  }
}
